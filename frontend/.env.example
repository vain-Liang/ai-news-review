# Optional. Leave unset in development to use the Vite proxy (/api -> http://127.0.0.1:8000).
# Set this in production if the frontend and backend are hosted on different origins.
VITE_API_BASE_URL=

# Optional. Override the backend admin API prefix when it is not served from /admin/.
VITE_ADMIN_API_PREFIX=/admin/
